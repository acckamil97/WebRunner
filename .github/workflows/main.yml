name: sfdx

on:
  workflow_dispatch:



jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    
#       - name: Salesforce SFDX CLI Action
#         uses: sfdx-actions/setup-sfdx@v1
#         with:
#           sfdx-auth-url: ${{secrets.SFDXAUTH}}
      - name: npm installation
        run: |
          npm install --location=global sfdx-cli@7.157.0
      - name: test sfdx
        run: |
          sfdx --version

          
          npm --version
